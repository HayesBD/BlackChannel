{
  "_comment": "Local dev only. The Functions host runs on :7071, so point the API there. In production, appsettings.json's apiBaseUrl is rewritten to the deployed Function URL at deploy time.",
  "apiBaseUrl": "http://localhost:7071/api"
}
